#include <stdio.h>
/**
 * main - Entry point
 *Return: Always 0 (Success)
 */

int main(void)
{

        int h = 97;
	int c = 65

        while (h <= 122)
        {
                putchar(h);
                        h++;
        }

	while (c <= 90);
	{
                putchar(c);
			c++;
	}
		putchar('\n');
return (0);
}

