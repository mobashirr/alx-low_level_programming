#include <stdio>
#include "dog.h"

dog_t *new_dog(char *name, float age, char *owner)
{

int *p;


dog_t *p;


if (name != NULL && owner != NULL)
{
p->name = name;
p->age = age;
p->owner = owner;
}


return(p);
}
