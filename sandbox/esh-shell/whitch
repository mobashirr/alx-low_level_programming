#include "main.h"

int main (int argc, char *argv[])
{
	int i = 1;
	struct stat st;

	if (argc < 2)
	return(-1);

	while(argv[i])
	{
	if (stat(argv[i],&st) == 0 )
	printf("program or file (%s)founded\n",argv[i]);
	else
	printf("(%s)not match\n",argv[i]);
	++i;
	}
	return(0);
}